# 家政服务商家联调手册

> 适用对象：接手新商家入驻联调工作的内部人员（开发/运营）。
> 商家侧的接口协议文档见 [api_doc.md](api_doc.md)（发给商家看的），本手册是 GR 侧的操作手册。

## 1. 手册目的与适用对象

家政版块（生活服务专区）支持多商家接入：平台（GR 侧）负责商品展示、下单、订单流转与 C 端展示，商家负责服务履约并通过接口与平台同步订单状态。

本手册用于指导新商家入驻的联调全流程，包含：测试环境地址、数据如何配置、联调步骤与方法、日志查看与问题排查、联调完成验收标准。**本手册不含任何密码、密钥等敏感信息**，凭证的获取途径见各章节说明。

文档分工：

| 文档 | 受众 | 内容 |
|------|------|------|
| [api_doc.md](api_doc.md) | 商家开发人员 | 商家需要实现的接口协议（HMAC 签名、订单回调、产品管理、商家需提供的两个接口） |
| 本手册 | GR 内部联调人员 | 测试环境、数据配置、联调步骤、日志排查、验收清单 |

## 2. 环境信息一览

| 项目 | 值 | 说明 |
|------|-----|------|
| 测试服务器 | `http://49.232.103.71:8765` | 部署目录 `/projects/beike`，服务端口 8765 |
| 测试库 | MySQL `62.234.26.57:3306`，库名 `juzhu` | 本地与测试服务器**共用同一实例**；账号密码在 `juzhu/.env` 的 `JUZHU_DB_*`（本手册不收录） |
| 本地启动 | `cd juzhu && python3 server.py` | 默认监听 8765 端口；本地日志 `/tmp/juzhu_server.log` |
| 代码发布 | `bash publish_test.sh` | rsync 同步代码到测试服务器并重启 8765 服务；脚本内含服务器凭证（本手册不收录） |
| 商家侧环境 | 商家自建 UAT | 由商家提供；接口地址配置到 `jz_vendors` 表（见第 3 章） |

**出站 IP 白名单**：平台调用商家接口（URL Link 生成、订单详情查询）时，商家接口通常按 IP 白名单鉴权。实际出站 IP 可从日志中的错误提示获取（形如 `IP无权访问-x.x.x.x`，见第 6 章），将其告知商家加入白名单。

**测试环境说明**：测试服务器本地起的 Python server（`juzhu/server.py`）与线上 Node 入口（`app.js`）功能对齐；本地与测试共用同一 MySQL 库，本地造数测试服务器同样可见。

## 3. 数据配置（新商家入驻）

### 3.1 关键表说明

| 表 | 作用 | 新商家需要关注的点 |
|------|------|------|
| `jz_vendors` | 商家主数据 | 新商家插入一行；`type`（cleaning/repair/moving/nanny 四选一，单值驱动 B 端产品列表）；`city_ids` 逗号分隔城市 id；`hmac_key`/`url_link`/`order_detail_url` 三列是接口联调核心配置 |
| `cities` | 城市 | 现有：1 沈阳 / 2 贵阳 / 3 北京 / 4 上海；新城市需插入后商家才能配置对应 city_id |
| `jz_skus` | 平台标准品（SPU） | 24 个平台标准品，`category_id` 对应四类；商家产品需关联 `channel_sku_id` |
| `jz_products` | 商家产品 | 商家通过 vendor 产品管理接口（api_doc.md 第 4 章）或 P 端管理台维护，**无需手工 SQL** |
| `gr_orders` | 订单 | 下单自动创建；联调时可造单验证（见第 5 章） |

数据库连接后自动迁移建表（`db.py` 启动自愈，无需手动建表/加列）；密钥配置已迁入 `jz_vendors` 表（原 `hmac_secret.key` 文件已废弃）。

### 3.2 新商家 INSERT SQL 模板

```sql
-- 新商家入驻：jz_vendors 插入一行（id 自增；参考现有商家 41=来来、42=蓝犀牛，建议 id 与 sort_order 同值）
INSERT INTO jz_vendors (
  type, name, logo, address, district_id, city_ids, phone,
  rating, review_count, rank_type, rank_label, badges, live,
  start_price, unit, fulfillment, hours, vendor_no, whitelist_id,
  hmac_key, url_link, order_detail_url, status, sort_order, created_at, updated_at
) VALUES (
  'cleaning',                    -- type：cleaning/repair/moving/nanny 四选一（必填）
  '新商家名称',                   -- name：C 端展示名（必填）
  NULL, NULL, NULL,              -- logo / address / district_id（可选，联调阶段可不填）
  '1,2',                         -- city_ids：逗号分隔城市 id（cities 表：1 沈阳 2 贵阳 3 北京 4 上海）
  NULL,                          -- phone：商家电话（可选）
  0, 0, NULL, NULL, NULL, 0,     -- rating / review_count / rank_type / rank_label / badges / live
  NULL, NULL, 'to_home', NULL,   -- start_price / unit / fulfillment / hours（可选）
  'V0043',                       -- vendor_no：商家编码（可选）
  NULL,                          -- whitelist_id：白名单关联（可选）
  '<64位hex密钥>',                -- hmac_key：与商家约定的 HMAC-SHA256 密钥（生成见 3.3）
  'https://<商家域名>/.../generate/urllink',  -- url_link：商家 URL Link 生成接口完整地址（必填）
  'https://<商家域名>/.../order/detail',      -- order_detail_url：商家订单详情查询接口（可选；不配则订单详情页不覆盖）
  'active', 43,                  -- status / sort_order（active=上架；建议 sort_order 与 id 一致）
  NOW(), NOW()                   -- created_at / updated_at
);
```

### 3.3 HMAC 密钥生成

```bash
openssl rand -hex 32   # 输出 64 位 hex 字符串
```

密钥也可由商家提供，双方约定一致即可；GR 侧将密钥写入 `jz_vendors.hmac_key`，商家侧用于请求签名（api_doc.md 第 2 章）。

### 3.4 配置生效与验证

1. **改表后必须重启 server**：密钥配置为进程内懒加载缓存，`jz_vendors` 表改动后需重启 server 才生效
2. 验证配置已加载：调用商家相关接口观察日志中的出站请求地址（见第 6 章 `[平台→商家]` 日志）
3. 平台标准品对照：
   ```sql
   SELECT id, name, slug FROM jz_skus WHERE category_id = '<type>';
   ```
   商家产品创建时需关联合适的 `channel_sku_id`（api_doc.md 4.6 创建产品）

## 4. 商家侧工作清单（转达给新商家）

联调开始前，把以下清单发给新商家对接人，约定交付物：

| # | 事项 | 说明 |
|---|------|------|
| 1 | 提供 HMAC 密钥 | 64 位 hex（或双方约定由 GR 侧生成后告知，见 3.3） |
| 2 | 实现 [api_doc.md](api_doc.md) 第 2 章 | HMAC-SHA256 请求签名与验签（签名步骤/参数过滤规则） |
| 3 | 实现 [api_doc.md](api_doc.md) 第 3 章 | 订单状态回调 `POST /api/juzhu/callback`（paid/assigned/serving/completed/cancelled 五种状态） |
| 4 | 实现 [api_doc.md](api_doc.md) 第 4 章 | 商家产品管理接口（城市/类目/SPU/产品 CRUD） |
| 5 | 提供 [api_doc.md](api_doc.md) 第 5.1 章接口 | 小程序 URL Link 生成接口（完整地址） |
| 6 | 提供 [api_doc.md](api_doc.md) 第 5.2 章接口 | 订单详情查询接口（可选但建议提供；完整地址；响应建议 5 秒内返回） |
| 7 | 白名单 | 将 GR 侧出站 IP 加入商家接口白名单（实际 IP 见第 2 章获取方式） |
| 8 | 测试小程序 | 提供测试环境小程序/链接，供端到端下单验证 |

> 提醒商家：api_doc.md 是正式接口协议，商家按该文档实现即可；联调中发现的文档歧义先与 GR 侧确认再改代码。

## 5. 联调步骤与方法

按以下顺序逐步推进，每步通过后再进下一步：

### 5.1 商家信息收集

向商家收集：商家名称、服务类型（type 四选一）、覆盖城市、HMAC 密钥、url_link 接口地址、order_detail_url 接口地址、测试小程序。填入第 3.2 节 SQL 模板执行。

### 5.2 GR 侧数据配置

1. 按第 3 章执行 INSERT SQL（`jz_vendors` 新商家一行）
2. **重启 server**（本地：kill 8765 端口进程后 `cd juzhu && python3 server.py`；测试服务器：`bash publish_test.sh`）
3. 验证商家数据可见：`GET /api/juzhu/jz/vendors`（新商家应出现在列表中，且不含 hmac_key 等敏感字段）

### 5.3 签名自测（GR 内部）

```bash
cd juzhu && python3 sign_test.py
```

本地验签工具（密钥读 `jz_vendors` 表，示例用 vendor 41），预期：

- 【服务端】正常请求校验结果: `True - 校验通过`
- 【服务端】被篡改数据校验结果: `False - 签名校验失败`

### 5.4 全接口联调（商家 → 平台）

```bash
cd juzhu && python3 test_vendor_api.py
```

打测试服务器 `49.232.103.71:8765` 的 22 个请求（城市/类目/SPU/产品 CRUD/回调五种状态），预期断言 `9/9 通过`。

- 可用 `JUZHU_TEST_BASE` 环境变量指定目标：`JUZHU_TEST_BASE=http://49.232.103.71:8765 python3 test_vendor_api.py`
- 脚本会创建并软删一个测试产品（正常现象）
- 脚本里商家固定为 vendor_id=41；新商家联调时可将脚本内 `VENDOR_ID` 改为新商家 id

### 5.5 下单全链路（平台 → 商家）

1. C 端下单（微信预约）：
   ```bash
   curl -s -X POST http://49.232.103.71:8765/api/juzhu/jiazheng/wechat-link \
     -H "Content-Type: application/json" \
     -d '{"product_id": <产品id>, "user_id": "<测试用户id>"}'
   ```
2. 平台调用商家 `url_link` 接口生成小程序链接（观察日志 `[平台→商家] POST ...`，见第 6 章）
3. 响应含 `url_link` 与 `order_ref`（GR 订单参考号，后续回调以此匹配）；`gr_orders` 落库一条 `pending` 订单

### 5.6 回调联调（商家 → 平台）

商家按 api_doc.md 第 3 章推送订单状态，联调时注意：

- 状态机：`pending → paid → assigned → serving → completed`；`cancelled` 为终态（可从任意状态取消）
- `paid` 回调必须带 `fee`（分）；平台落库后写入 `vendor_oid`，**后续回调必须 `order_ref` + `vendor_oid` 同时匹配**，否则返回 404「订单不存在」
- `assigned` 回调必须带 `worker`（name/phone/eta）；eta 格式为北京时间无时区（如 `2026-08-17 09:30:00`，平台兼容 ISO 8601 带时区格式）
- `cancelled` 回调必须带 `cancel_reason`
- 回调接口带 HMAC 签名（用新商家密钥），未签名/密钥错误返回 401

### 5.7 订单详情同步（平台 → 商家）

用户进入订单详情页时，平台静默调用商家 `order_detail_url`（`GET ...?order_ref=xxx`）并将结果同步到本地订单（状态向前推进、补 `vendor_oid`/`fee`/`worker`/`cancel_reason`）。联调验证：

```bash
curl -s "http://49.232.103.71:8765/api/juzhu/gr/orders/<order_ref>/vendor-detail?user_id=<测试用户id>"
```

- 返回 `ok:true` 且 `detail.status` 与商家侧一致即打通
- 商家未配置 `order_detail_url` 时返回 `ok:false`（详情页保持本地数据，属正常）
- 该接口失败是**静默**的（用户无感知），联调时以服务器日志为准

### 5.8 C 端回归

```bash
cd juzhu && python3 test_gr_my_orders.py
```

覆盖用户下单/列表（过滤 pending）/详情/vendor-detail 同步，预期 `ALL PASS`。

### 5.9 造单示例（可选）

联调中需要指定商家/用户的订单时：

```python
# cd juzhu && python3
from tp_client import load_dotenv
load_dotenv()
import db
from gr_orders import create_order
conn = db.connect()
create_order(conn, 'GRTESTS<自定义后缀>', '99', city='沈阳', vendor_id=<新商家id>, user_id='<测试用户id>')
conn.commit(); conn.close()
```

## 6. 日志查看与问题排查

### 6.1 日志位置

| 环境 | 日志路径 | 查看命令 |
|------|---------|---------|
| 本地 | `/tmp/juzhu_server.log` | `tail -f /tmp/juzhu_server.log` |
| 测试服务器 | `/tmp/beike_server.log` | ssh 登录 `49.232.103.71`（凭证见 publish_test.sh）后 `tail -f /tmp/beike_server.log` |

出站调用日志（平台 → 商家）格式：

```text
[平台→商家] 2026-08-17 14:01:17 POST https://<商家域名>/.../generate/urllink
      >> 参数: {"path": ..., "query": ..., "order_ref": ...}
      << 返回: {"code": 200, ...}
```

- `>>` 为请求参数，`<<` 为商家返回；`!` 开头为异常信息
- `JUZHU_LOG_DETAIL=false` 时只打印请求 URL 一行（在 `juzhu/.env` 配置）
- 商家回调请求（商家 → 平台）无专门日志，可通过接口响应码判断（401=签名失败、404=订单不存在）

### 6.2 常见问题排查表

| 现象 | 可能原因 | 处理 |
|------|---------|------|
| 商家回调返回 401 | 签名错误：密钥不一致/参数过滤规则不符/timestamp 格式 | 对照 api_doc.md 第 2 章核对；用 sign_test.py 自测 |
| 商家回调返回 404「订单不存在」 | `order_ref` 或 `vendor_oid` 不匹配 | paid 回调后才写入 vendor_oid；后续回调必须两者同时匹配 |
| 出站日志 `IP无权访问-x.x.x.x` | 商家接口 IP 白名单未包含平台出站 IP | 把 x.x.x.x 告知商家加白名单 |
| 出站日志 SSL/CERTIFICATE 错误 | 商家证书链不完整 | 平台已放宽证书校验（白名单兜底），若仍失败确认商家接口可达性 |
| 订单状态未推进 | 状态机顺序错误（如跳过 paid） | 按 5.6 状态机检查回调顺序 |
| 订单详情页未显示商家数据 | 商家未配 `order_detail_url` 或接口静默失败 | 查 jz_vendors 配置；看服务器日志出站记录；手动 curl 验证 vendor-detail |
| 改表后配置不生效 | 密钥配置为懒加载缓存 | 重启 server（见 5.2） |
| 下单返回 `IP无权访问` | 商家 url_link 接口白名单未放开 | 同上告知商家加白名单；同时确认产品 `query` 非空（商家可能要求） |

## 7. 联调完成验收清单

新商家联调结束前逐项确认：

- [ ] 商家信息（密钥/两接口地址/白名单）已配置 `jz_vendors` 且重启生效
- [ ] `sign_test.py` 签名验签通过、篡改拦截生效
- [ ] `test_vendor_api.py` 22 请求断言全部通过
- [ ] 五种回调状态正确落库（paid 金额 / assigned 服务者+eta 北京时间无时区 / cancelled 原因）
- [ ] 下单成功：商家 url_link 接口返回链接、`gr_orders` 创建订单
- [ ] C 端"我的订单"可见且状态正确（列表/详情页）
- [ ] 订单详情同步生效（vendor-detail 返回与商家一致，下次查询即最新数据）
- [ ] 日志无异常堆积（无 SSL/超时/签名错误）
- [ ] 本手册第 3-5 章步骤无需口头解释即可由另一人执行

## 8. 安全约定

1. **本手册不含任何密码/密钥**；数据库凭证在 `juzhu/.env`（已被 .gitignore 屏蔽），服务器凭证在 `publish_test.sh`
2. 密钥不进 git：`.key`/`.env*` 已被 .gitignore 屏蔽；收到商家真实密钥只写入 `jz_vendors.hmac_key`，**禁止写入文档/代码/聊天群**
3. 平台 vendor 对外接口不返回 `hmac_key`/`url_link`/`order_detail_url`（已剥离），联调验证时不要将这些值出现在可公网访问的页面
4. 测试库为共享库，联调造数用明显测试标记（如 `GRTESTS` 前缀），避免污染真实数据
