# C 端联调测试手册（生活服务专区）

> 适用对象：GR 侧 C 端测试人员（产品/测试/开发均可执行，**无需后台与数据库操作**）。
> 商家侧接口联调、数据配置、日志排查见《[联调手册](联调手册.md)》（内部 GR 侧手册）；接口协议见 [api_doc.md](api_doc.md)（发给商家的）。
> 本手册只覆盖 **C 端用户视角**的完整链路：页面浏览 → 下单跳小程序 → 订单状态推进核对。

---

## ⚠️ 特别提醒（测试前必读）

### 提醒一：C 端未接真实登录，所有人是同一个模拟用户

**当前 C 端没有真实登录体系，user_id 是前端模拟值，且首页每次加载都会强制重置为 `demo_user_001`。**

由此产生以下现象，**均为预期行为，不是 bug**：

1. **所有人打开 C 端"我的订单"看到的订单完全一样**——A 手机下的单，B 手机的订单列表里立刻可见
2. 并行测试会互相干扰：同事刚推完状态的订单，会出现在你的列表里"自己动"
3. 看到"不认识的订单"不要当串单上报——那是所有联调人员共享的订单池

需要独立测试时（避免和别人混单）：

- 手机浏览器（或电脑浏览器 F12 手机模式）控制台执行：
  ```js
  BZF_JZ.setUserId('my_test_001')   // 换成自己的标记，如姓名拼音
  ```
- **注意：一旦回到首页（index.html），user_id 会被强制重置回 `demo_user_001`**。隔离测试期间请在订单/详情/列表页之间跳转，不要回首页；回到共享池测试时重新访问首页即可

### 提醒二：必须用手机系统浏览器打开，禁止在微信内打开

- 测试地址：`http://49.232.103.71:8765`（手机需可访问公网）
- **不要把链接发到微信里直接点开**。原因：下单后页面跳转的是 `weixin://dl/business/...` 协议链接（用于唤起来来小程序），微信内置浏览器会拦截该协议跳转，表现为**点"下一步"后提示无活动**
- 正确姿势：用手机自带浏览器（iOS Safari / 安卓 Chrome）复制地址访问。下单时系统会弹出 **"在微信中打开"确认框**，点击确认唤起来来小程序——看到该弹框即为跳转正常
- 建议把地址收藏/生成二维码，用手机相机扫码后在浏览器打开

---

## 1. 测试前准备

| 项目 | 要求 |
|------|------|
| 测试设备 | 手机（iOS/安卓），系统浏览器可上网；或电脑 Chrome F12 手机模拟（跳小程序环节仍需真机） |
| 测试地址 | `http://49.232.103.71:8765`（首页 index.html，默认定位沈阳） |
| **来来小程序体验用户** | **开测前必须提前在联调群中联系来来同事，把测试人员的微信号加入小程序体验用户名单**——测试版小程序未加体验成员无法打开，跳转后会提示无权限/无法访问。加好后用测试手机微信确认能打开来来小程序 |
| 对接人 | 来来侧联调对接人（创建商品、推动订单状态，见第 2、4 章话术） |

**话术模板⓪（开测前，发到联调群）：**

> @来来-对接人 你好，C 端联调测试即将开始，麻烦将来来小程序的体验用户名单加上以下测试同事的微信号（加好后群里知会一声，我们确认小程序可打开）：
> - {测试人员微信号 1}
> - {测试人员微信号 2}

## 2. 前置条件核对（联系来来创建商品）

C 端商品按 **城市 + 类目** 双维度展示：某城市无上架商品时，详情页整页显示"当前城市暂无该服务"。所以走查前先确认目标城市有商品。

现有城市：**1 沈阳 / 2 贵阳 / 3 北京 / 4 上海**；现有商家：**41 来来（保洁，覆盖沈阳/贵阳/北京）、42 蓝犀牛（搬家，沈阳/贵阳）**。

**话术模板①（发给来来对接人，直接复制修改）：**

> @来来-对接人 你好，C 端联调准备中，麻烦在测试环境确认/创建以下商品：
> - 商家：来来；品类：保洁（cleaning）
> - 城市：沈阳（city_id=1）为主，贵阳（2）/北京（3）可选
> - 要求：商品状态为上架（on），已关联平台标准 SPU
> 创建好后回复一下商品所在城市和名称，我们开始 C 端走查。

来来创建商品后，按下面两步核对（均无需数据库权限）：

### 2.1 第一步：B 端商品页核对字段（/screens/p-jz-product.html）

电脑或手机浏览器打开：`http://49.232.103.71:8765/screens/p-jz-product.html`（B 端运营商工作台·商家 SKU 页，只读查看无需任何凭证）：

1. 在筛选栏选择：**商家 = 来来、城市 = 目标城市（如沈阳）、状态 = 上架**，也可用搜索框按商品名搜索
2. 核对表格中该商品每一列是否符合与来来的约定：

| 核对项 | 预期 | 不符合时的后果（C 端表现） |
|--------|------|--------------------------|
| 城市 | 目标城市 | C 端切到该城市看不到商品 |
| 引用 SPU | 显示蓝色 SPU 标签（**不能是灰色"未关联"**） | 商品不出现在 C 端列表/详情 |
| 状态徽章 | 绿色"上架"（不能是下架/售罄） | C 端不展示 |
| 价格 | 与约定一致（元） | C 端列表/详情价格错误 |
| 服务者 | 人数 ≥ 0（可暂不要求） | 仅影响详情页服务者展示 |

3. （建议）点该行 **"编辑"** 按钮——仅弹出详情表单不会修改数据——核对 **小程序路径 path、小程序参数 query 均非空**：这两个值是下单跳转来来小程序的关键参数，缺失会导致下单后无法正确进入商品页。核对完点"取消"关闭弹窗

> ⚠️ 本页只做查看，**不要点击"保存""删除"或状态徽章**（写操作需管理员凭证，失败或误改都会影响联调数据）。

### 2.2 第二步：C 端可见性验证

手机打开首页 → 切到"生活服务专区" → 点"保洁" → 列表能刷出服务卡片、价格正常，即商品就绪；列表为空说明商品未覆盖当前城市（回到话术①或第一步排查字段）。

## 3. C 端页面全流程

### 3.1 首页（index.html）

1. 打开 `http://49.232.103.71:8765`，顶部三段切换：**保租房专区 / 卖旧买新专区 / 生活服务专区**，点"生活服务专区"
2. 顶部城市栏（"沈阳 · 新居住专区 ▾"）可切换城市；也可用 URL 直链指定城市（如 `index.html?city=贵阳`）。若顶部城市栏不可点击（被后台配置关闭），统一用 URL 直链方式切城市
3. 核对四个类目卡片：**保洁 / 维修 / 搬家 / 保姆**，点击进入对应列表页
4. "我的订单"栏显示四个状态入口（已支付/已派单/服务中/已完成）及数量角标，点角标直达对应状态的订单列表

### 3.2 列表页（juzhu-jiazheng-list.html?type=类目）

1. 顶部子类筛选（如保洁下的"日常保洁/深度保洁…"），切换后列表刷新
2. 每张服务卡片显示：服务名、价格、标签。**注意：列表页价格是该服务在所有城市在售商品的最低价**（未按当前城市过滤，可能低于当前城市实际可选价）；当前城市的准确价格以进入详情页后底部 CTA 展示为准
3. 点任意卡片进入详情页

### 3.3 详情页（juzhu-jiazheng-detail.html?sku=xxx）

1. 核对服务信息、价格、商家介绍、评价区
2. **多商家比价**："其他商家同款"区块可点击切换不同商家（同款不同价）；同商家多套餐/多规格也可切换，底部价格随选中商品联动
3. 底部 CTA 显示当前选中商品价格，右侧按钮为 **"下一步"**
4. 异常表现记录：若整页显示"当前城市暂无该服务"→ 商品未覆盖当前城市（回到第 2 章）

### 3.4 下单（详情页点"下一步"）

1. 点击"下一步"，出现遮罩提示 **"正在生成预约链接…"**（平台正在调来来的链接生成接口）
2. 成功后页面跳转，系统弹出 **"在微信中打开"确认框** → 确认 → 唤起来来小程序
3. **在来来小程序内完成预约与支付**（此环节在商家侧操作，支付金额自定）
4. 支付成功后来来侧推 `paid` 回调，C 端订单即生效（见第 4 章）

> 下单瞬间 C 端看不到订单号（页面直接跳走了）。拿订单号的方法：**先让来来推 paid → C 端"我的订单"出现该单 → 进详情页复制完整订单号**（详情页"订单号"一行）。

## 4. 订单状态联调（联系来来推单）

C 端订单状态由 **来来侧回调推动**，GR 测试员只需把订单号发给来来并按步骤核对展示。

状态机：`pending（C 端不可见）→ paid → assigned → serving → completed`；`cancelled` 为终态（任意状态可取消）。

**话术模板②（下单后发给来来对接人）：**

> @来来-对接人 我已完成一笔下单，请先推 paid 回调（fee 建议 12800 即 ¥128，便于核对）。推完后：
> 订单号：{从订单详情页复制的完整订单号，GR 开头}
> 请按以下顺序继续推进，每推一步回复一声，我在 C 端核对展示：
> 1. assigned（服务者：姓名/手机号/预计上门时间）
> 2. serving（服务中）
> 3. completed（已完成）

### 状态用例表（每推一步，在 C 端核对）

| # | 来来侧动作 | 必带信息 | 订单列表页预期 | 订单详情页预期 |
|---|-----------|---------|--------------|--------------|
| 0 | （下单完成，未推 paid） | — | **列表不可见**（pending 不展示，属正常） | — |
| 1 | 推 paid | 支付金额 fee（分） | "全部"和"已支付"出现该单，金额按分转元显示（如 12800 → ¥128） | 步骤条走到"已支付"（高亮当前步）；订单金额、订单号、下单/支付时间正确 |
| 2 | 推 assigned | 服务者姓名、电话、预计上门时间 | 状态变为"已派单" | "服务者"行显示 姓名·电话；"预计上门"显示时间；步骤条走到"已派单" |
| 3 | 推 serving | — | 状态变为"服务中" | 步骤条走到"服务中"，显示服务中时间 |
| 4 | 推 completed | — | 状态变为"已完成" | 步骤条全部完成，显示完成时间 |
| 5 | （可选）推 cancelled | 取消原因 | 状态变灰色"已取消"（只在"全部"里，无独立 tab） | 顶部红色提示条："该订单已取消：{原因}"，步骤条置灰 |

核对入口：
- **订单列表**：首页"我的订单"→"全部订单"，或直接访问 `juzhu-jiazheng-orders.html`
- **订单详情**：列表卡片点"查看详情"；顶部步骤条直观显示进度

> **进详情页会自动同步最新状态**：每次打开订单详情页，平台会静默调来来的订单详情接口刷新本地数据。若来来侧状态已变而 C 端列表没刷新，**重新进一次详情页**即可拿到最新（同步失败用户无感知，列表仍显示旧状态）。

## 5. 测试注意事项

1. **模拟用户共享**（重申提醒一）：所有人共享 `demo_user_001` 订单池；判断某单是不是"自己的"，看下单时间与服务名，或按提醒一设置独立 user_id
2. **城市影响商品与订单**：商品按城市过滤（切到北京看到的是北京的商品）；订单列表不按城市过滤（全部展示）
3. **金额单位**：接口与回调金额单位为"分"，C 端自动转元展示（¥128 = 12800）；若来来推的金额与展示不符，先确认单位
4. **cancelled 单**：列表页无"已取消"tab，在"全部"里核对；首页订单栏也无取消入口
5. **演示模式开关**（首页"演示模式：开/关"）：默认关闭即可，与本手册流程无关

## 6. 验收清单

- [ ] 手机系统浏览器打开测试地址，三专区切换正常，城市可切换
- [ ] 测试人员微信号已加入来来小程序体验名单，微信可正常打开来来小程序
- [ ] B 端商品页（screens/p-jz-product.html）核对通过：商家/城市/SPU 已关联/上架状态/价格正确，path/query 非空
- [ ] 来来已就绪目标城市商品：列表页出卡、价格正确
- [ ] 详情页多商家/多规格切换，CTA 价格联动
- [ ] 点"下一步"弹出"在微信中打开"确认框，确认后进入来来小程序
- [ ] 小程序内完成预约支付，来来推 paid 后 C 端订单列表可见且金额正确
- [ ] assigned：详情页显示服务者姓名·电话、预计上门时间
- [ ] serving / completed：状态与步骤条逐级推进
- [ ] cancelled（可选）：列表灰色"已取消"+详情红色原因条
- [ ] 重新进详情页能拿到来来侧最新状态（静默同步）
- [ ] 已知提醒确认：共享订单池现象已知悉，未误报串单
