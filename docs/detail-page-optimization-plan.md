# 旅居详情页优化清单（lvju-app-detail + booking 闭环）

> 2026-09-06 立项。背景：详情页已完成携程式改版（日期条 + 房型卡 + 整屏房态日历 + 底栏咨询/查看房型），
> 房态按「户型×月份」缓存、每卡显示逐晚实价合计、含已订区间标「满」。
> 本清单承接后续优化项。**「周边玩法」由另一会话并行处理，不在本清单内。**
> 每项完成后在本文件勾选状态；涉及约定沉淀的回写 CLAUDE.md。

## P0 · 闭环与可信度

### 1. 下单页改造为「确认单」式 —— `lvju-app-booking.html` ✅ 2026-09-06
- [x] 顶部确认卡：项目 + 所选户型 + 入住/离店/晚数（只读展示，带「修改」链接回详情页日历）
- [x] 费用卡：晚均价 × 晚数 = 总价；可展开逐晚价格明细（读 stay-calendar 实价，与详情页同口径）
- [x] 保留既有提交链路不动：`POST /api/juzhu/booking`、账号 token、常用联系人、min-stay 服务端兜底、
      minsu 预付收银台 / rental 预订单分流（实测 rental 出单 BKG-RENTAL-00127）
- [x] 视觉与详情页统一（青绿 + 金、卡片圆角、青绿渐变主按钮）
- 验收：从详情卡片「订」进入，所见即所选；改日期只能回详情日历；提交成功页不回归老表单

### 2. 日历选段跨已订夜校验 —— `lvju-app-detail.html` ✅ 2026-09-06
- [x] 选离店时逐晚检查选中户型房态，区间含已订/关房 → 拒绝并提示「X月X日 已订，请调整」（实测 93：9/6→10/3 提示「9月10日 已订」，确定保持禁用）
- [x] 与卡片「满」态、服务端下单校验三处口径一致（规则 16）
- 验收：9/6 → 9/23（院落大床房 9/22 已订）无法成段；观山套房可正常成段

### 3. 假数据换真数据（除「周边玩法」）—— `lvju-app-detail.html` ✅ 2026-09-06
- [x] `document.title` 随项目名更新
- [x] hero 轮播接真图：photos scroll-snap 横滑 + 计数角标（cap 8 张）；无图回退 cover_image；单图隐藏计数
- [x] 管家卡：读 `units.keeper`（admin 户型详情已可维护）；无 keeper 数据 → 整块隐藏（93/9041 已验证不再显示「杨晓芳」）
- [x] 房源验真卡：rating 通过时用真实评级编号 + 二维码；未通过/无编号 → 隐藏（93 评级草稿态已验证隐藏）
- 验收：id=93 / 9041 / 95 页面无一处与该项目无关的演示信息；无 id 示例基线保持原样

### 4. 加载骨架防闪变 —— `lvju-app-detail.html` ✅ 2026-09-06
- [x] 带项目 id 进入时，房型卡先渲染骨架（灰块微光），数据到达后替换；失败显示错误态（节流网络实测不闪示例卡）
- 验收：慢速网络下不再闪现静态示例卡

## P1 · 观感与细节

### 5. appbar 假按钮激活 ✅ 2026-09-06
- [x] ghost 栏 ♡ 与房型区收藏联动（同一 localStorage `bzf_fav`，双向同步）
- [x] ⤴ 分享：`navigator.share`（标题 + 链接），不支持时降级复制链接 + 按钮反馈

### 6. 日期条「晚数」一行化 ✅ 2026-09-06
- [x] mid 列改一行「共 47 晚」

### 7. 日历弹层手势与导航 ✅ 2026-09-06
- [x] 顶部拖拽把手 + 下滑手势关闭（阈值 80px，实测 touch 手势关闭）
- [x] 「回到本月」悬浮 pill：滚离当月后出现，点击回当月

### 8. admin 商家咨询方式列表过滤 ✅ 2026-09-06
- [x] `GET /admin/vendors/consult` 只返回有房源项目的商家（`HAVING project_count > 0`，42 → 10 家）

### 9. 房态批量接口（后端 + 前端接入）✅ 2026-09-06
- [x] `GET /api/juzhu/projects/:id/stay-calendar?month=X&units=u1,u2` 一次返回该月所有户型房态
      （`{units:[{unit_id, base_price_night, days}]}`），不带 units 的旧形状保持兼容
- [x] 详情页 loadMonth 改为每「月」一个请求（93：原来 2 户型×N 月 = 2N 请求 → 现在 N 个）

### 10. 房型卡照片 ✅ 2026-09-06
- [x] `units.cover_image` 有图用图（项目 12 实测 7/7 卡带图），无图回退渐变色块

## 暂缓（记录在案，本轮不做）
- 收藏服务端同步：依赖用户体系，先 localStorage
- 无障碍（日历格子 aria、弹层焦点管理）：原型期后置
- `_region.js` 区域抽象：lvju 为贵州演示基线，不适用
- 「周边玩法」真数据：**另一会话并行处理中**
